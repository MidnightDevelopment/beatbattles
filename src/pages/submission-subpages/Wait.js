import {Steps, Button, message, Row, Col, Typography, Form, Input} from 'antd';
import React from 'react';
import './Wait.less'
const { Title, Link } = Typography;

const Wait = () => (
    <>
        <Title className={'wait-title'}>Time Remaining 23:59:59</Title>
    </>
);

export default Wait;